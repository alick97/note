"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const tmpdir_1 = require("../fs/tmpdir");
const init_1 = __importDefault(require("../init"));
describe("initBook", () => {
    let dir;
    beforeEach(() => {
        dir = (0, tmpdir_1.createTmpDirWithRealPath)("honkit-init-test-");
    });
    test("should create a README and SUMMARY for empty book", () => {
        return (0, init_1.default)(dir).then(() => {
            expect(dir).toHaveFile("README.md");
            expect(dir).toHaveFile("SUMMARY.md");
        });
    });
});
